rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Reports: users can only access their own files
    match /reports/{uid}/{reportFile=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
} 